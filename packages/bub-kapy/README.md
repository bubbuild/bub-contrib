# bub-kapy

Kapybara integration for `bub`.

## Installation

```bash
uv pip install -e packages/bub-kapy
```

## Usage

This plugin registers a `kapy` model to `bub`.

```bash
bub --model kapy "Hello Kapybara!"
```
