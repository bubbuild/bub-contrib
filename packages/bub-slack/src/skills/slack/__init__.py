"""Slack skill package.

Ships as part of the ``bub-slack`` distribution under the ``skills`` PEP 420
namespace package so Bub auto-discovers it (``import skills`` -> ``slack``) in any
environment where ``bub-slack`` is installed. This module exists only so the build
backend detects the package; the skill itself is described by ``SKILL.md``.
"""
